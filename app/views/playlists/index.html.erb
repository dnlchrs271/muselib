<p id="notice"><%= notice %></p>

<h1>Listing Playlists</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @playlists.each do |playlist| %>
      <tr>
        <td><%= playlist.title %></td>
        <td><%= playlist.artistname %></td>
        <td><%= playlist.string %></td>
        <td><%= playlist.shuffleplay %></td>
        <td><%= playlist.string %></td>
        <td><%= link_to 'Show', genre_playlist_path(id: @genre.id) %></td>
        <td><%= link_to 'Edit',   edit_genre_playlist_path(id: @genre.id) %></td>
        <td><%= link_to 'Destroy', genre_playlist_path(id: @genre.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New', new_genre_playlist_path(genre_id: @genre.id) %>

